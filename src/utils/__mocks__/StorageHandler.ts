import { OptionsToStorage, StorageToOptions } from "utils/definitions";

const StorageHandler = jest.genMockFromModule("utils/StorageHandler");

export { StorageHandler, OptionsToStorage, StorageToOptions };
